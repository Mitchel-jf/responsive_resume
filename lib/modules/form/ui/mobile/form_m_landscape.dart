import 'package:flutter/material.dart';

class FormMobileLandscapePage extends StatelessWidget {
  const FormMobileLandscapePage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}