import 'package:flutter/material.dart';

class FormDesktopPage extends StatelessWidget {
  const FormDesktopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('Preview'),
      ),
    );
  }
}
