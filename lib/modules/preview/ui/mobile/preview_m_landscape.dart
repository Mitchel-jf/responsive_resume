import 'package:flutter/material.dart';

class PreviewMobileLandscapePage extends StatelessWidget {
  const PreviewMobileLandscapePage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}